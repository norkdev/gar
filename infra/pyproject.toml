[project]
name = "gar-infra"
version = "2.1.0"
description = "AWS CDK infrastructure for Governed Agentic Retrieval"
requires-python = ">=3.13"
dependencies = [
    "aws-cdk-lib",
    "constructs",
]
