[project]
name = "gar-infra"
version = "0.1.0"
description = "AWS CDK infrastructure for Governed Agentic Retrieval"
requires-python = ">=3.14"
dependencies = [
    "aws-cdk-lib",
    "constructs",
]
